"use client";

/**
 * testimonial.demo.tsx — minimal wrapper showing the testimonial
 * component in isolation. Mount this in a route for visual review,
 * e.g. `app/mockups/testimonial/page.tsx`:
 *
 *   import DemoOne from "@/components/ui/testimonial.demo";
 *   export default function Page() { return <DemoOne />; }
 */
import Example from "@/components/ui/testimonial";

export default function DemoOne() {
  return <Example />;
}
